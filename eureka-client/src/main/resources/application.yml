spring:
  application:
    name: hello-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#  instance:
#    instance-id: ${spring.application.name}:${server.port}
server:
  port: 8082

#日志
logging:
  level:
    cn.aki.demo: debug