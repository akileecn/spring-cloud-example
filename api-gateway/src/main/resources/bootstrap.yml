spring:
  application:
    name: api-gateway
  cloud:
    config:
      #默认根据uri
      discovery:
        enabled: true
        service-id: config-server

server:
  port: 5555

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

#开放/refresh接口
management:
  security:
    enabled: false
