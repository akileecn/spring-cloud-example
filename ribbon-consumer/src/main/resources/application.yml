spring:
  application:
    name: ribbon-consumer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 9000

#日志
logging:
  level:
    cn.aki.demo: debug

